---
name: dev-constraints
description: 开发约束纪律：先规划确认再编码、简单优先、手术式修改、真实测试验收、不写死逻辑、严格控制 token、读懂源码再动手、拒绝假测试、分步检查点。本技能应在任何编码任务开始时使用，尤其是修改现有代码、添加功能、修复 Bug 的场景。
---

# 开发约束纪律

在任何编码任务中严格执行以下流程与规则。规则之间冲突时，以顺序靠前的规则优先。

## 阶段 0：规划与确认（编码前必须完成）

1. **先思考再编码**：动手前先分析任务，输出一份需求清单（目标、涉及文件、改动点、验证方式）。
2. **等待确认**：需求清单必须得到用户明确确认后才能开始写代码。用户未回复时不得擅自开工。
3. **读懂源码再动手**：修改前必须先读相关代码，理解现有逻辑、依赖关系与风格约定。禁止凭猜测修改。

## 阶段 1：设计原则

4. **简单优先**：代码越少越好。不写用不上的功能、不写防御性冗余、不预留"以后可能用到"的抽象。
5. **只做判断，不写死逻辑**：逻辑根据输入/状态在运行时判断，禁止硬编码特定值、特定路径、魔法数字来"绕过"问题。
6. **代码冲突时不妥协**：两种方案冲突时，不写折中的混合方案；选择正确的一方，向用户说明取舍理由。

## 阶段 2：修改纪律

7. **手术式修改**：只改与任务直接相关的代码。
   - 不顺手重构，不改变量名，不调整无关格式。
   - 不删注释，不删"看似没用"的代码，不动无关文件。
   - 改动范围以需求清单为边界，超出边界的发现只报告、不动手。

## 阶段 3：检查点与验收（未通过不得报告完成）

8. **分步设检查点**：多步任务每完成一步，向用户简报该步结果（改了什么、怎么验证的），再进入下一步。
9. **目标导向的测试**：改完代码必须运行真实测试验证。
   - 测试用例覆盖主流程、边界条件、异常输入。
   - 运行项目已有的测试命令；没有则写最小验证脚本或用命令行直接验证。
10. **拒绝假测试**：
    - 禁止为了通过而修改断言、删测试、注释测试。
    - 禁止只测恒真条件（如 `assert True`、捕获异常后跳过）。
    - 禁止声称"应该能跑"——必须贴出实际运行输出或退出码作为证据。
11. **严格控制 token 消耗**：不做重复读取、不输出大段无关代码、报告只写结论与证据，不贴全文。

## 阶段 4：诚实汇报

12. **遇到问题绝不隐瞒**：测试失败、无法验证、方案有风险、改错了代码——第一时间如实告知用户，附上错误信息与当前状态，等待用户决策。禁止掩盖失败、假装成功。

## 快速自检（每次任务结束前过一遍）

- [ ] 需求清单经用户确认了吗？
- [ ] 读懂了被修改代码的上下文吗？
- [ ] 改动只限于任务范围吗？
- [ ] 真实测试跑过并有输出证据吗？
- [ ] 有没有隐瞒的问题？
